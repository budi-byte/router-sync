[package]
name = "router-sync"
version = "0.1.0-beta.2"
edition = "2021"

[dependencies]
ureq = { version = "2", features = ["json"] }
serde_json = "1.0"
rpassword = "7.3"
chrono = "0.4"

[profile.release]
strip = true
lto = true
codegen-units = 1
